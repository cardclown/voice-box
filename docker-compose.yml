version: "3.8"

services:
  java-maven:
    build:
      context: .
      dockerfile: Dockerfile.java-maven
    container_name: java-maven-3.6-jdk8
    working_dir: /workspace
    volumes:
      - .:/workspace
      - maven-repo:/root/.m2
      - ../tmpVideo:/tmpVideo
    stdin_open: true
    tty: true
    environment:
      - VOICEBOX_DB_URL=jdbc:mysql://voice-box-mysql:3306/voicebox_db?useSSL=false&allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=utf-8
    command: ["bash", "/workspace/dev-watch.sh"]
    # 默认 bridge 网络即可访问外网，不做额外限制

  mysql:
    image: mysql:5.7
    platform: linux/amd64
    container_name: voice-box-mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: voicebox_db
    ports:
      - "13308:3306"
    volumes:
      - mysql-data:/var/lib/mysql

volumes:
  maven-repo:
    driver: local
  mysql-data:
    driver: local



